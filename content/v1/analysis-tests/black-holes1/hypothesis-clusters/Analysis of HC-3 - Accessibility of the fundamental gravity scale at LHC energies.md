---
type: cluster-review
cluster: "[[HC-3 - Accessibility of the fundamental gravity scale at LHC energies]]"
---

## What the analysis says

Question: is the fundamental gravity scale ~TeV, so LHC collisions could make black holes at all? Members: [[H-3 - Two or more large compact extra dimensions lower the fundamental gravity scale to about a TeV]], [[H-8 - The fundamental gravity scale is far above LHC energies so no black holes form]], residual [[H-9 - TeV-scale gravity arises via some mechanism not listed here]]. Prior [0.03, 0.94, 0.03] = posterior — zero likelihood blocks; all evidence sits in the prior. The single inbound edge [[E-5 - O-8 × HC-3 — the observed hierarchy is what large extra dimensions are built to explain]] (the 16-order hierarchy, `used_for_prior: true`) enters via `p_tev_resolution = 0.4`; [[A-4 - Gauss-law dilution of gravity in large extra dimensions reproduces weak 4D gravity from a TeV fundamental scale]] (approved/checked) merely removes a knockdown, while its relocation caveat caps `share_gravity_scale = 0.15`. The prior's own sensitivity note: a naturalness skeptic sends H-8 to ~1; an advocate at 0.8 still leaves H-8 ≈ 0.88.

## What the model may not capture

1. The prior comment promises "case-specific LHC search results are ... left to step 8", but no such observation node exists and step 8 wrote no likelihood — so the strongest post-1998 evidence (LHC monojet/missing-energy nulls excluding M_* up to ~5-10 TeV, per [[H-3 - Two or more large compact extra dimensions lower the fundamental gravity scale to about a TeV]]'s own prose; sub-mm torsion-balance limits) never entered any computation. It pushes the same direction as the prior (toward H-8), so the posterior is directionally safe but understated in confidence — a defect shipped unfixed.
2. `share_gravity_scale = 0.15` and `share_add = 0.5` are proposal-space head-counts — a reference class picked for availability; theory-space shares track publication fashion, not truth.
3. Exclusivity is soft: hybrid geometries (warped + large) blur H-3/H-9, though both land on the "production possible" side, so the split barely matters downstream. An unlisted answer here (H-9) is *enabling*, i.e. as consequential as H-3 — the residual's 0.03 does real risk-relevant work on a guessed number, and the true load-bearing quantity is the combined H-3+H-9 ≈ 0.06 low-scale mass.

## What would help

1. LHC extra-dimensions/black-hole search limits as a priced observation — *exists, unread* (largest effect: would push H-3 and H-9 down further).
2. Post-1998 sub-mm gravity tests (Eöt-Wash torsion-balance limits) — *exists, unread*.
3. A principled measure over hierarchy-resolution space to replace the head-count shares — *does not exist*.

## Confusions and contradictions

No genuine conflict: the one observation (the hierarchy) points weakly toward low-scale gravity, and everything else is omitted rather than opposed. The only tension is internal — H-3's own prose cites exclusions the arithmetic never saw (item 1 above), which has a nameable fix and so lives in parts 2-3, not here.
